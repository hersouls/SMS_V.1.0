# 🔐 Google OAuth 환경 변수 설정

# =============================================
# Supabase Configuration
# =============================================
# Supabase 프로젝트 URL
# Supabase Dashboard → Settings → API → Project URL
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key
# Supabase Dashboard → Settings → API → anon public key
REACT_APP_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================
# Google OAuth Configuration
# =============================================
# Google Cloud Console에서 생성한 OAuth 2.0 클라이언트 ID
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# =============================================
# Site Configuration
# =============================================
# 프로덕션 사이트 URL
REACT_APP_SITE_URL=https://your-domain.com

# OAuth 콜백 URL (일반적으로 사이트 URL + /auth/callback)
REACT_APP_SUPABASE_AUTH_REDIRECT_URL=https://your-domain.com/auth/callback

# =============================================
# 개발 환경용 추가 설정 (선택사항)
# =============================================
# 디버그 모드 활성화
REACT_APP_DEBUG_MODE=true

# 로그 레벨 설정
REACT_APP_LOG_LEVEL=debug

# =============================================
# 설정 방법:
# =============================================
# 1. 이 파일을 .env로 복사하세요: cp .env.example .env
# 2. 위의 값들을 실제 값으로 변경하세요
# 3. .env 파일은 Git에 커밋하지 마세요 (.gitignore에 추가됨)
# 4. 프로덕션 환경에서는 환경 변수를 직접 설정하세요

# =============================================
# 주의사항:
# =============================================
# - REACT_APP_ 접두사가 붙은 변수만 클라이언트 앱에서 사용 가능
# - 민감한 정보 (Client Secret 등)는 클라이언트에 노출되지 않도록 주의
# - Google Client Secret은 Supabase Dashboard에서만 설정하고 여기에는 포함하지 않음